(document.onload = (() => {
    setTimeout(() => {
        let menu = document.getElementById('fading-in-menu');
        menu.style.transition = '2s';
        menu.style.opacity = 1;
    }, 1000);
}))();


